
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/room_ready_db
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.error.include-stacktrace=never


app.room-ready.app-name:room-ready
app.room-ready.jwt-secret:${JWT_SECRET:sangatrahasia}
app.room-ready.jwt-expirationInSecond:${JWT_EXPIRATION_IN_SECOND:600}
